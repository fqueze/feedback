- profiler-cli: 'thread markers --search name:CPU Use --list' gives one row per 100ms sample with no value column; to get the CPU% range over a zoomed test I needed --json + script. Question: 'what was machine CPU over this test's duration'. Command: profiler-cli thread markers --search 'name:CPU Use' --list --limit 0 (zoomed to m-1). Could have shown: min/median/max of cpuPercent in the list header, or the field value in each row. Also the resource-usage profile reports 'No counters in this profile' so CPU is only reachable via these markers.
