- `profiler-cli thread markers --category Test --search <test> --list --limit 0` (text mode): every marker shows `t=1m23s`; a whole test's log collapses to 2 distinct timestamps, so ordering relative to DOMEvents cannot be read. Expected ms precision (e.g. t=83.029s) when the profile is longer than a minute. Workaround: `--json` and read `.flatMarkers[].start`. Question it could not answer: at what ms did the failing assertion happen relative to the key event.
- The `bmtc-1` daemon died silently between two commands (after `zoom push 83.0125,83.0265`, `thread samples-top-down` twice (once `--include-idle`), both printing an empty tree, then `zoom pop`). The log ends with 'Client connected' and no error; next command said 'daemon exited without cleaning up'. Expected: an error in the log, or the daemon staying up. Workaround: reload the profile (marker handles lost).
- `thread samples-top-down` in a 14 ms zoom (83.0125,83.0265 on a GeckoMain) printed only the header 'Top-Down Call Tree:' and nothing else, with and without `--include-idle`. Expected: '0 samples in range' so it reads as no data rather than as a truncated output.
- `profiler-cli profile info --session bmtc-3` right after a successful `load` of a 1m24s CI profile: 'Timed out after 30000ms waiting for the daemon ... to answer.' The next command worked. Expected: the load to return only once the daemon answers, or profile info to wait. Workaround: rerun.
- `profiler-cli load <url> --session <id>` with no PROFILER_CLI_SESSION_DIR set, under the sandbox: 'EPERM: operation not permitted, mkdir /Users/florian/.profiler-cli'. The message suggested PROFILER_CLI_SESSION_DIR=/Users/florian/.sandbox/tmp/profiler-cli, which worked (other agents already use it). Expected: nothing to change in the tool; the brief could say to set it, since every agent in this sandbox hits it.
- (review) `profiler-cli load "https://profiler.firefox.com/from-url/<aNBzGHNsSv-pqzhv6PDC_w per-test profile>/marker-table/?marker=1381717&thread=0&v=17" --session review-bmtc-1b`: printed 'Error: Profile load timeout after 60000ms (set PROFILER_CLI_LOAD_TIMEOUT_MS to override)', yet the very next `zoom push` and `thread markers --session review-bmtc-1b` answered normally. Expected: a load that reports success once the session answers, or a session that is really gone. Workaround: ignore the error and query, or set PROFILER_CLI_LOAD_TIMEOUT_MS=240000.
- (review) `profiler-cli load https://firefox-ci-tc.services.mozilla.com/api/queue/v1/task/aNBzGHNsSv-pqzhv6PDC_w/runs/0/artifacts/public/test_info/profile_browser_multiselect_tabs_close.js.json --session review-bmtc-1c` selected t-157 (GeckoMain, Privileged Content), where loading the profiler link to the same profile selected t-0 (GeckoMain, Parent Process). The next `thread markers --search "tabclose" --list --json` returned 0 markers with no hint that the thread was the wrong one. Expected: the raw load to default to the parent process main thread, as the link load does, or the thread named in every marker listing. Workaround: `thread select t-0`.
