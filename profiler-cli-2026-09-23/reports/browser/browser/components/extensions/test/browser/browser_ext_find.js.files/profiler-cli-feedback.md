- `profiler-cli thread markers --search "waiting for paint" --list --limit 0 --json` then a python histogram per second: question was 'from when does this repeating marker fire continuously (60/s)?'. The default --list output shows only the first N; the aggregate 'Frequency Analysis' gives avg/max intervals but not when the steady rate began. A per-bucket rate or 'first start of a steady run' would have answered it.
- (review) Question: "which document do these `RefreshDriverTick waiting for paint` markers belong to?" `profiler-cli thread markers --session <s> --thread t-22 --search "innerWindowID:4294967331"` returned 0 markers, though `marker info --json` shows `innerWindowID` in `rawFields` of those very markers. Workaround: `--list --limit 0 --json` and a python Counter over `data.innerWindowID`. Expected: `--search innerWindowID:<id>` to match (or a `--group-by innerWindowID`), and the default list/aggregate to show the page URL resolved from the inner window ID.
