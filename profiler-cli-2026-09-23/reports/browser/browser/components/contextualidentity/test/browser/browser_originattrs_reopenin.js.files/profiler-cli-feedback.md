- `profiler-cli screenshots --session browser-reopenin-1 --at 20.0 -o <dir>`: expected only the windows on screen at t=20s, to tell whether `tabContextMenu` was still open. Got 5 windows, including the `context_reopenInContainerPopupMenu` submenu (last frame t=14.236s) and `tab-preview-panel` (last frame t=14.379s), both hidden by then according to their `popuphidden` markers. A closed popup window and an open one that simply stopped repainting look the same, so the screenshot could not settle "is the menu still open". Workaround: relied on the absence of `popuphiding` for `tabContextMenu`. Could show: when each window was last shown or closed, or leave out windows that are closed at t.
- `profiler-cli thread samples --session browser-reopenin-2 --include-idle` over a 660 ms zoom: expected running vs idle sample counts, to tell whether the main thread was busy when a 500 ms timer fired 1.1 s late. Got "Categories (41 running samples)" with no idle count and no total, so the busy fraction was unknown. Workaround: none; left the timer lateness unexplained.
