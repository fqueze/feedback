- Question: which windows' refresh drivers tick, and does one observer reason appear on every tick of one window over a time range. Command: `profiler-cli thread markers --search name:RefreshDriverTick --list --limit 0 --session browser-890262-1`; the text list shows each tick's reasons but not its innerWindowID, so telling the main window's ticks from other windows' needed --json and a script. A --group-by field:innerWindowID that works on RefreshDriverTick (`--group-by field:reasons` returned '(no value)' for all 410), or innerWindowID in the list row, would have answered it.
- Correction to the entry above: 'field:reasons' was my guess; the payload keys are 'name' (the tick reasons text) and 'innerWindowID', so '(no value)' was expected. What remains: the --list row does not show innerWindowID, and nothing in the default output says which payload keys a marker has without 'marker info'.
- (review) `profiler-cli --session browser-review-890262-1 thread markers --search vsync --list --limit 0` failed with "error: unknown option '--session' (Did you mean --version?)". Expected: a global option accepted before the subcommand, as the guide's "pass --session on every call" suggests. Workaround: put `--session` after the subcommand's own options. Also hit the same innerWindowID question as the first entry, for the same ticks.
