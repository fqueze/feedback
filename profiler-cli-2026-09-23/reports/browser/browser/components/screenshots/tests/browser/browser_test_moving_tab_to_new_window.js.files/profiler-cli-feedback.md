- Question: "in which order, and how many ms apart, did these content-process markers happen?" Command: `profiler-cli thread markers --thread t-95 --list --limit 0 --session browser-moving_tab-1` after `zoom push 79.715,79.765` (a 50 ms view). Expected: start times at a resolution matching the view (e.g. t=79.7390s). Got: every row `t=1m20s`, so a resize DeferredTask, a getBoundingClientRect flush and the next rAF 20 ms later were indistinguishable in time. Workaround: `--json` and read `flatMarkers[].start` with a script, and `marker info m-A m-B ... --json` for handles. The list could print times with enough digits for the zoomed range.
