## test_frameactor-03.js (2026-09-22)

- **Question: "were this test's failures part of whole-job breakages?"** `fx-tests test devtools/server/tests/xpcshell/test_frameactor-03.js --task-ids --limit 0` listed 6 failing tasks and "6x TIMEOUT Test exceeded time limit". Expected: some hint that each of those 6 jobs had 477-1564 failing tests (a mass failure, where the test is irrelevant). Got: nothing. I had to run `fx-tests task <id>` on all six to find out. Could show: the job's failing-test count next to each task id, or a verdict line such as "all 6 failures are in jobs where >400 tests failed".
- **Question: "which bug are these jobs starred with?"** `fx-tests test <path> --bugs` found no bug, because bug 1991833 ("High frequency win-msix PermissionError: [WinError 5] Access is denied") does not name the test. I found it with a Bugzilla quicksearch. Could show: the bug(s) sheriffs attached to the failing jobs themselves (`fx-tests intermittent --bug 1991833` lists LKys8j8iTQKTAN9hKv040g, one of this test's failing tasks).
- **Question: "did the process ever run?"** The issue says TIMEOUT. The harness logged "will retry" 30-55 ms after TEST-START, and the 45 s "TIMEOUT" is an uncancelled kill timer after a failed launch. `fx-tests task` shows only "Timed out and was force-killed by the harness". Could show: the gap between TEST-START and the thread ending, when it is far below the timeout.
