- Question: "which bugs name this test?" Command: `fx-tests test browser/components/screenshots/tests/browser/browser_test_moving_tab_to_new_window.js --bugs`. Expected: a bug list, or a line saying none was found. Got: output identical to the run without `--bugs`, no bug section at all, so "no bug" and "flag ignored" look the same. Workaround: `fx-tests intermittent --test <path>` (which does say "no sheriff-annotated bug names the test") and reading the bug named in the manifest's skip-if.
