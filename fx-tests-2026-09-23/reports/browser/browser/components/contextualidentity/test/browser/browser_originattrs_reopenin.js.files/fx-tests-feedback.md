- Question: "in the failing jobs, which test ran just before this one, and did it fail?" (tells an order dependency or a restart-after-timeout apart). `fx-tests test <path> --task-ids` gives only task ids, so I ran `fx-tests task <id>` on 5 non-standalone jobs one at a time and grepped them: every one also had `browser_newtabButton.js` TIMEOUT, the test just before this one in the manifest. Could show: for each failing task, the previous test in the same browser and its status, or a count of "other tests that failed in the same jobs".
