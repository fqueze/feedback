- Question: "are the latest failures on revisions from before or after the fix landed?" `fx-tests test <path> --history` and `--task-ids` bucket failures by the day the task ran, with no revision or push date. The 3 failures shown on 2026-09-14 were macOS jobs run on Aug 15 pushes (a78ff5819100, da79ac063c12, 54cd4c62c282), i.e. backfills of pre-fix code, which made the test look like it still failed 10 days after the fix. I had to query the Taskcluster task (`payload.env.GECKO_HEAD_REV`) and the Treeherder push API for each task to find out. Showing the revision (and push date, or a "run on an older push" flag) next to each task ID in `--task-ids`, or bucketing `--history` by push date, would have answered it directly.
